export default class Pizza {
    constructor(id, name, categoria, ingredientes, preco, imageUrl) {
      this.id = id;
      this.name = name;
      this.categoria = categoria;
      this.ingredientes = ingredientes;
      this.preco = preco;
      this.imageUrl = imageUrl;
    }
  }